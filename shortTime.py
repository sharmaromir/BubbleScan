import Test

from Test.finalGrader import export
import os
from Test.pdfToJPG import convertToImg


export(file_name="updated.png", options=27, startValue=8, numQuestions=3, outputFileName='test.csv')
#this is a testing code, may not work as intended in the future.